class ApiConfig {
  // Ajusta o IP/URL de acordo com o teu backend (ex: http://localhost:8080/api)
  static const String baseUrl = 'http://localhost:8080/api';
  // Nota: Se usares um telemóvel físico, em vez de 'localhost' deves usar o IP da tua máquina na rede local (ex: 'http://192.168.1.50:8080/api')
}